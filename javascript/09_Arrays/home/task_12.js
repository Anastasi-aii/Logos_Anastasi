function unique(arr) {
    for (let elem of Array.from(arr)) {
        return Array.from(strings);
    }
  }
  
  let strings = new Set (["кришна", "кришна", "харе", "харе",
  "харе", "харе", "кришна", "кришна", ":-O"
]);

    console.log(unique(strings));