function unique(arr) {
    for (let elem of Array.from(arr)) {
        return Array.from(values);
    }
  }
  
  let values = new Set (["Hare", "Krishna", "Hare", "Krishna",
    "Krishna", "Krishna", "Hare", "Hare", ":-O"]);

    console.log(unique(values));
  